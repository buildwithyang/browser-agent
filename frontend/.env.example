# 开发不需要设置 VITE_GATEWAY_URL：前端走同源 /api，由 Vite 反向代理到网关
# （代理目标见 VITE_PROXY_TARGET，默认 http://127.0.0.1:17321）。
#
# 仅「生产静态部署」时才设置它为网关的绝对地址（此时没有 Vite 代理）：
# VITE_GATEWAY_URL=https://your-gateway.example.com

# 可选：覆盖 Vite /api 代理的网关目标（开发用）。
# VITE_PROXY_TARGET=http://127.0.0.1:17321
